# PUBLIC_INTERFACE
# REACT frontend configuration example.
# Base URL for API requests. Defaults to same-origin '/api' if not set.
# For local backend at different port, e.g. http://localhost:5000
REACT_APP_API_BASE=/api
